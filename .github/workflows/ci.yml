name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  linux-build-and-test:
    name: Linux gRPC build and tests
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            pkg-config \
            protobuf-compiler \
            libprotobuf-dev \
            libgrpc++-dev \
            protobuf-compiler-grpc \
            libssl-dev \
            openssl
      
      - name: Verify Protobuf installation
        run: |
          protoc --version
          pkg-config --modversion protobuf
          find /usr -name "ProtobufConfig.cmake" 2>/dev/null || echo "Config not found"
      
      - name: Configure CMake
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTING=ON
      
      - name: Build
        run: |
          cmake --build build --target \
            accel_server \
            accel_client_a \
            accel_client_b \
            accel_unit_tests \
            accel_model_tests \
            accel_tls_tests
      
      - name: Run tests
        run: |
          ctest --test-dir build --output-on-failure
